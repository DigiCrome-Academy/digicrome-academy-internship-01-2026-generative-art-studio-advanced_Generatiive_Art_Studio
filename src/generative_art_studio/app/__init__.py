"""Phase 4: the unified Generative Art Platform.

`model_registry.py` holds the platform's pure, testable logic (model
lookup, sample generation, gallery state, high-res export). `streamlit_app.py`
is the interactive UI built on top of it — run it with:

    streamlit run src/generative_art_studio/app/streamlit_app.py
"""
